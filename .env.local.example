# GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens
# Required scopes: read:user  (no repo access needed)
# This is NEVER exposed client-side — only used in API route handlers.
GITHUB_TOKEN=ghp_your_token_here
